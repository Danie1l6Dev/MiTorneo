{{-- Needs: $signature (base64 data URI) --}}
<div class="signature">
    <img src="{{ $signature }}" alt="Firma del coordinador">
    <div class="line"></div>
    <div class="role">Coordinador</div>
</div>
